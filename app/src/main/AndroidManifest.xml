<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== 權限聲明 ==================== -->

    <!-- 精確鬧鐘權限（Android 12 = API 31-32） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 精確鬧鐘權限（Android 13+ = API 33+，不須使用者手動授權） -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- 通知權限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 全螢幕通知權限 -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 震動權限 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 喚醒裝置權限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 開機自動啟動權限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 前台服務權限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"
        android:minSdkVersion="34" />

    <!-- 電池優化白名單請求（直接跳出系統對話框詢問使用者） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".NexAlarmApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NexAlarm"
        tools:targetApi="31">

        <!-- ==================== Activities ==================== -->

        <!-- 主 Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NexAlarm"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Gemini 整合 URI Scheme -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nexalarm" />
            </intent-filter>

            <!-- 標準鬧鐘 Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SET_ALARM" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 全螢幕鬧鐘 Activity -->
        <activity
            android:name=".ui.screens.AlarmRingingActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.NexAlarm.FullScreen"
            tools:targetApi="o_mr1" />

        <!-- ==================== Services ==================== -->

        <!-- 鬧鐘服務 -->
        <service
            android:name=".service.AlarmService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            tools:targetApi="34" />

        <!-- 會議模式快速設定磚片 -->
        <service
            android:name=".service.MeetingModeTileService"
            android:exported="true"
            android:icon="@drawable/ic_meeting_mode"
            android:label="@string/meeting_mode"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- ==================== Receivers ==================== -->

        <!-- 鬧鐘觸發接收器 -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.nexalarm.app.ALARM_TRIGGER" />
                <action android:name="com.nexalarm.app.ALARM_SNOOZE" />
                <action android:name="com.nexalarm.app.ALARM_DISMISS" />
            </intent-filter>
        </receiver>

        <!-- 開機自動啟動接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
